請把這張「醫療救護紀錄單」影像**完整轉寫**為 HTML,不要摘要、不要解釋、不要輸出 HTML 以外的文字。

這是給後續程式解析用的轉寫,請嚴格遵守:

1. **逐字照抄**影像上所有印刷文字與手寫文字,包含區塊標題(如「檢傷分類」「生命徵象」「過去重要病史」「主要初步診斷」「創傷」「非創傷」)。不要改寫或翻譯。
2. **勾選框**一律輸出為 `<input type="checkbox">`(未勾選)或 `<input type="checkbox" checked>`(已勾選)。打勾、打叉、塗黑、圈選都算已勾選。
   勾選框請放在**該選項名稱的前面**,順序與影像一致,例如:
   `<input type="checkbox"> 1 撕裂傷 <input type="checkbox" checked> 2 表淺損傷`
3. **標籤與值同列**時用 `標籤:值` 形式,例如 `姓名:陳大明`、`體溫:36.5`、`血壓:132/84`。
   欄位空白就只寫標籤與冒號,不要自行填入內容。
4. 表格用 `<table><tr><td>`,同一列的欄位維持在同一個 `<tr>` 內。
5. 手寫文字若無法辨識,寫 `<span>?</span>`,**不要猜測、不要編造**。
6. 保留數字原樣(含小數點與單位),不要換算或修正。
   意識欄若寫成「15(E4V5M6)」這種「總分 + 括號內分項」的形式,**兩者都要完整轉寫**,
   不可只留總分或只留括號內容。
7. 人形圖上的標記不需轉寫。

只輸出 HTML。
